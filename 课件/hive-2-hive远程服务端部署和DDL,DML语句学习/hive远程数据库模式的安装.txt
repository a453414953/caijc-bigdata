安装hive的步骤：
node02：
	1、拷贝hive-2.3.4到node02和node03
		cd /opt/bigdata 
		scp -r hive-2.3.4 node03@`pwd`
		scp -r hive-2.3.4 node04@`pwd`
服务端node03：
客户端node04：
	2、修改环境变量
		vi /etc/profile
		export HIVE_HOME=/opt/bigdata/hive-2.3.4
		将bin目录添加到PATH路径中
服务端node03：
	3、修改配置文件，进入到/opt/bigdata/hive-2.3.4/conf
		为区分node02上的hive，修改第一条和第二条配置，将hive改为hive_remote，结果如下
			<property>
				<name>hive.metastore.warehouse.dir</name>
				<value>/user/hive_remote/warehouse</value>
			</property>
			<property>
				<name>javax.jdo.option.ConnectionURL</name>
				<value>jdbc:mysql://node01:3306/hive_remote?createDatabaseIfNotExist=true</value>
			</property>
			<property>
				<name>javax.jdo.option.ConnectionDriverName</name>
				<value>com.mysql.jdbc.Driver</value>
			</property>
			<property>
				<name>javax.jdo.option.ConnectionUserName</name>
				<value>root</value>
			</property>
			<property>
				<name>javax.jdo.option.ConnectionPassword</name>
				<value>123</value>
			</property>
服务端node04：
	3、修改配置文件，进入到/opt/bigdata/hive-2.3.4/conf
		mv hive-default.xml.template hive-site.xml
		保留第一个配置，并修改hive为hive_remote:
			<property>
				<name>hive.metastore.warehouse.dir</name>
				<value>/user/hive_remote/warehouse</value>
			</property>
		增加如下配置信息：
			<property>
				<name>hive.metastore.uris</name>
				<value>thrift://node03:9083</value>
			</property>
服务端node03：
	4、执行初始化元数据数据库的步骤
		schematool -dbType mysql -initSchema
服务端node03：
	5、启动服务
		nohup hive --service metastore &
